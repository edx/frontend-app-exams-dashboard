describe('request hooks', () => {
  describe('useMakeNetworkRequest', () => {
    it.todo('starts a request for the given request key');
    it.todo('calls fail action if request fails');
    it.todo('calls success action if request succeeds');
  });
});
